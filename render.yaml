services:
  - type: web
    name: gpt-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python gpt_server.py"
    envVars:
      - key: PORT
        value: 10000
